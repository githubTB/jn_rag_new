"""
产品结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "product_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取近三年的产品生产情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从表格或半结构化文本中提取“产品类年度数据”。

你需要识别表格结构（行与列的关系），并进行规范化转换，而不是简单提取数字。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“产品相关的年度数量数据”，输出JSON数组。

【通用数据识别规则】
1. 如果文本呈现为表格：
   - 第一列通常为“产品名称”（如品名、名称、产品等）
   - 其他列可能为“年份”或“时间维度”（如2021年、2022年等）

2. 如果检测到“年份列”：
   - 必须将每一行拆分为多条记录（每个年份一条）

3. 如果是普通文本：
   - 提取“产品名称 + 年份 + 数值”的组合

【字段定义】
   产品名称
   年份
   数量
   数量单位
   产值
   产值单位
   工业增加值
   工业增加值单位

【重要规则】
1. 优先提取“具体产品”，避免使用泛化类别（如“疫苗”、“活疫苗”等），除非文本中没有更细粒度名称
2. 如果一行包含多个年份数据，必须拆分为多条记录
3. 忽略明显的汇总行（如“总计”、“合计”）
4. 忽略与生产数量无关的数据（如产能、设计能力等）
5. 保持数值原样，不进行推算

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 缺失字段填 null

文本：
{text}
""".strip()
