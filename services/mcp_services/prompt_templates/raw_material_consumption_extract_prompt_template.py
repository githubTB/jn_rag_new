"""
主要原辅材料消耗结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "raw_material_consumption_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的主要原辅材料消耗情况、材料名称、消耗量、单位、消耗环节及单位产品消耗量"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业生产或能源报告文本中提取主要原辅材料消耗信息，并进行规范化结构输出。

你需要识别材料名称、消耗数据及其对应的生产环节，并提取单位产品消耗情况。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“主要原辅材料消耗信息”，并输出为JSON数组。

【字段定义】
- 名称：原材料或辅助材料名称
- 消耗量：材料的总消耗量
- 单位：消耗量对应单位（如吨、kg、m³等）
- 消耗环节：材料消耗所在的生产环节（如生产、加工、辅助等）
- 单位产品消耗量：单位产品对应的材料消耗量
- 备注：其他补充说明信息

【抽取规则】
1. 每一种材料对应一条记录
2. 如果为表格结构，按列映射字段
3. 如果为文本描述，需识别“材料名称 + 消耗量 + 单位 + 环节 + 单位产品消耗”
4. 数值保持原始值，不做计算或换算
5. 缺失字段填 null
6. 忽略汇总行或无关描述

【识别关键词】
原材料、辅料、消耗、用量、单位产品消耗、生产环节、备注

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    名称
    消耗量
    消耗量单位
    消耗产线
    来源
    备注

文本：
{text}
""".strip()