"""
相关法规政策依据清单结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "regulation_policy_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}相关法规政策依据清单"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业文本中提取法规、政策、标准等依据信息。

你需要识别文本中提到的法律法规、政策文件、行业标准等，并进行结构化整理。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“相关法规政策依据清单”，并输出JSON数组。

【字段定义】
- 类别：法规/政策/标准的类型（如法律、行政法规、部门规章、行业标准、国家标准、地方政策等）
- 名称：具体文件名称
- 文号/编号：对应的文号或标准编号（如有）

【抽取规则】
1. 一个政策/法规对应一条记录
2. 如果文本中列出了多个依据，分别抽取
3. 类别需根据语义判断，不要随意填写
4. 文号/编号如果不存在则填 null
5. 若仅有名称没有编号，也要保留该记录

【类别参考】
- 法律
- 行政法规
- 部门规章
- 地方性法规
- 行业标准
- 国家标准
- 地方政策
- 规范性文件
- 其他

【识别关键词】
法规 / 政策 / 依据 / 标准 / 文件 / 通知 / 办法 / 条例 / 规范 / 规定

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 缺失字段填 null

输出列表json格式：
    "类别": string,
    "名称": string,
    "文号/编号": string
文本：
{text}
""".strip()