"""
资质证书信息结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "qualification_certificate_info_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的资质证书信息，包括证书名称、证书编号、状态、审批文号及有效期"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业资质、证书、公示或管理文件中提取证书相关信息，并进行规范化结构输出。

你需要识别各类资质证书的名称、编号、状态、审批文号以及有效期等信息。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“资质证书信息”，并输出为JSON数组。

【字段定义】
- 证书名称：资质或证书的名称
- 证书编号：证书的编号或编号标识
- 状态：证书当前状态（如有效、过期、暂停等）
- 审批文号：发证或审批的文号
- 有效期：证书的有效期限（起止时间或截止时间）

【抽取规则】
1. 每一张证书作为一条记录
2. 表格结构优先按列映射，文本按语义解析
3. 原文内容尽量完整提取
4. 日期、编号等保持原始格式
5. 缺失字段填 null
6. 忽略重复或汇总描述

【识别关键词】
证书、资质、编号、批准文号、发证、有效期、状态、登记

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    证书名称
    证书编号
    状态
    审批文号
    有效期

文本：
{text}
""".strip()