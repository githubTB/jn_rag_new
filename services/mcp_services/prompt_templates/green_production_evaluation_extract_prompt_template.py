"""
绿色生产水平综合评价结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "green_production_evaluation_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的绿色生产水平评价、评价维度、当前状态、主要问题及提升潜力"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业绿色制造、节能环保评估或综合评价文本中提取评价维度及对应分析内容，并进行规范化结构输出。

你需要识别不同评价维度对应的当前状态、存在问题以及提升潜力等信息。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“绿色生产水平综合评价”，并输出为JSON数组。

【字段定义】
- 评价维度：评价的具体维度（如能源、资源、环境管理、污染控制等）
- 当前状态：该维度的现状描述
- 主要问题：存在的主要问题或不足
- 提升潜力：改进方向或提升空间

【抽取规则】
1. 每个评价维度视为一条记录
2. 表格结构优先按列映射，文本按语义解析
3. 原文中描述需尽量完整提取，不要随意概括
4. 缺失字段填 null
5. 忽略重复或汇总性描述

【识别关键词】
绿色、评价、维度、现状、问题、不足、改进、提升、潜力

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
评价维度、当前状态、主要问题、提升潜力

文本：
{text}
""".strip()