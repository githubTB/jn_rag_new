"""
管理体系现状结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "management_system_status_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的管理体系现状、体系建立情况、三方认证、认证时间、存在不足及改进建议"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业管理相关文本或报告中提取管理体系现状信息，并进行规范化结构输出。

你需要识别不同管理体系的类型及其建立与认证情况，并提取对应的不足与改进建议。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“管理体系现状信息”，并输出为JSON数组。

【字段定义】
- 管理体系：如质量管理体系、环境管理体系、职业健康安全管理体系等
- 建立情况：是否已建立、运行情况等描述
- 三方认证：是否通过第三方认证（如ISO认证等）
- 认证时间：认证获得或最近一次认证的时间
- 主要不足：当前体系存在的主要问题或缺陷
- 改进建议：针对不足提出的改进措施或建议

【抽取规则】
1. 每一种管理体系对应一条记录
2. 如果文本为列表或分段描述，需分别提取
3. 数值或时间保持原始文本表达，不做格式转换
4. 如果信息未明确提及，对应字段填 null
5. 忽略无关描述或重复信息

【识别关键词】
质量管理、环境管理、职业健康安全、能源管理、体系、认证、ISO、审核、不足、改进

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    管理体系
    建立情况
    三方认证
    认证时间
    主要不足
    改进建议

文本：
{text}
""".strip()