"""
能源计量器具信息结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "energy_metering_instrument_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的能源计量器具信息、计量设备名称、型号、准确度、数量及检定结果"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业能源管理相关文本或表格中提取能源计量器具信息，并进行规范化结构输出。

你需要理解计量层级结构（一级用能单位、二级次级用能单位、三级用能设备），并准确对应各层级的器具信息。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“能源计量器具信息”，并输出为JSON数组，如未提供则默认为空。

【数据结构说明】
该信息通常按照层级划分：
- 一级用能单位
- 二级主要次级用能单位
- 三级主要用能设备

每一层级包含器具名称、型号、准确度、数量及检定结果等信息。

【字段定义】
- 能源或耗能工质品种名称：如电、水、蒸汽、天然气等

一级用能单位：
- 一级用能单位器具名称
- 一级用能单位器具型号
- 一级用能单位准确度
- 一级用能单位数量
- 一级用能单位检定结果

二级主要次级用能单位：
- 二级主要次级用能单位器具名称
- 二级主要次级用能单位器具型号
- 二级主要次级用能单位准确度
- 二级主要次级用能单位数量
- 二级主要次级用能单位检定结果

三级主要用能设备：
- 三级主要用能设备器具名称
- 三级主要用能设备器具型号
- 三级主要用能设备准确度
- 三级主要用能设备数量
- 三级主要用能设备检定结果

【抽取规则】
1. 按层级分别提取，不要混淆一级、二级、三级数据
2. 表格按列映射字段，文本按语义识别归类
3. 每一类器具信息尽量完整提取
4. 数值保持原始值，不做计算或转换
5. 缺失字段填 null
6. 忽略无关描述或汇总信息

【识别关键词】
能源、计量、器具、仪表、型号、准确度、检定、用能单位、电表、水表、流量计等

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    能源或耗能工质品种名称
    一级用能单位器具名称
    一级用能单位器具型号
    一级用能单位准确度
    一级用能单位位数量
    一级用能单位检定结果
    二级主要次级用能单位器具名称
    二级主要次级用能单位器具型号
    二级主要次级用能单位准确度
    二级主要次级用能单位数量
    二级主要次级用能单位检定结果
    三级主要用能设备器具名称
    三级主要用能设备器具型号
    三级主要用能设备准确度
    三级主要用能设备数量
    三级主要用能设备检定结果

文本：
{text}
""".strip()