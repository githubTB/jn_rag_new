"""
水泵设备能效对标结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "pump_efficiency_benchmark_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的水泵设备信息、型号、数量、容量、损耗及能效对标情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取与设备能效分析专家，擅长从企业文本或表格中提取水泵设备相关参数，并进行规范化结构化输出。

你需要识别水泵设备的关键指标，并提取对应的能效对标结果，而不是简单复制文本。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“水泵设备能效对标信息”，并输出为JSON数组。

【字段定义】
- 型号：设备型号
- 数量：设备数量
- 数量单位：如台、套等
- 容量：水泵流量或输送能力等参数
- 容量单位：如m³/h、L/s等
- 损耗：设备损耗或能耗指标（如存在）
- 企业水泵：企业实际水泵效率或能耗指标
- 企业水泵单位：对应单位（如%或其他）
- 一级能效值：一级能效标准值
- 二级能效值：二级能效标准值
- 三级能效值：三级能效标准值
- 能效对标结果：根据企业水泵指标与标准对比得出的结果（如一级能效、二级能效、不达标等）
- 评价标准：文本中提到的引用标准或评价依据（如国家标准、行业标准等）

【抽取规则】
1. 一行数据对应一个水泵设备记录
2. 表格需按列映射字段
3. 文本描述需识别“型号 + 容量 + 能效指标 + 对标信息”
4. 数值保持原始值，不进行计算或转换
5. 忽略汇总行（如“合计”“总计”）
6. 缺失字段填 null
7. 若未明确对标结果，可根据描述判断；无法判断则为 null
8. 若未明确评价标准，则填 null

【识别关键词】
水泵、泵、型号、容量、流量、损耗、效率、能效、对标、标准

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
型号、数量、数量单位、容量、容量单位、损耗、企业水泵、企业水泵单位、一级能效值、二级能效值、三级能效值、能效对标结果、评价标准

文本：
{text}
""".strip()