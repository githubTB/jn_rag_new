"""
企业基本信息结构化抽取配置。
"""

SERVICE_NAME = "base_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取{company_name}的营业执照信息、企业基本信息、财务信息"

# system 提示词
system_prompt = "你是一个数据提取分析师，基于提供的文本，提取出其中的数值信息和关联数值信息。"

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：
    你需要从提供的文本中抽取相关信息，并输出一个JSON对象。

输出字段：
    企业名称
    统一社会信用代码
    法人代表
    成立时间
    注册地址
    现生产地址
    企业性质
    所属省市
    所属区县
    行业
    是否重点行业
    是否上市企业

要求：
    - 输出json格式
    - 不要解释
    - 不要markdown
    - 不要换行格式化
    - 缺失字段值为""
    - 所有字段名用""括起来

文本：
{text}
""".strip()

