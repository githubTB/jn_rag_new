"""
能源结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "energy_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取近三年的能源消耗数据，包括电力、天然气、蒸汽、煤、柴油、汽油、生物质、热力等能源品种的实物量、折标系数、当量值、等价值，重点关注能源消费统计表、综合能耗表、能源平衡表、能源汇总表"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业能源统计表、综合能耗表、能源平衡表和相关说明文字中提取“近三年能源消耗”数据。

你需要优先理解表格的行列关系，将“能源品种 + 年度”拆分成独立记录，并准确保留实物量、折标系数、折标量及单位信息。
""".strip()

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：
从文本中提取“近三年能源消耗”数据，并输出为JSON数组。

【字段定义】
- 能源品种：如电力、天然气、蒸汽、原煤、柴油、汽油、热力、生物质等
- 年度：如 2021、2022、2023、2024
- 实物量：原始消耗量数值
- 实物量单位：如 kWh、万kWh、Nm3、m3、t、kg、GJ 等
- 当量值折标系数：仅提取数值（必须去除单位，如 kgce/kW·h），例如 0.1229
- 当量值：当量折算后的数值
- 当量值单位：如 tce、kgce
- 等价值折标系数：仅提取数值（必须去除单位，如 kgce/kW·h），例如 0.3015
- 等价值：等价值折算后的数值
- 等价值单位：如 tce、kgce

【抽取规则】
1. 每个“能源品种 + 年度”组合视为一条记录
2. 如果表格中一行包含多个年份（如 2021/2022/2023），必须拆分为多条记录
3. 优先理解表头与列结构，按列年份进行映射
4. 只提取能源消耗相关数据，禁止混入：
   - 取水量
   - 原材料消耗
   - 排放数据
5. “合计 / 总计 / 其中”等项：
   - 只有在明确为能源总量时才保留
   - 否则忽略
6. 所有数值保持原始值，不做换算、不推断、不补全
7. 缺失字段一律填 null
8. 折标系数字段（当量值折标系数、等价值折标系数）必须：
   - 仅保留纯数字
   - 不允许包含任何单位、字母、斜杠或符号
   - 示例：0.1229（正确），0.1229 kgce/kW·h（错误）

【识别关键词】
    能源品种
    年份
    实物量
    实物量单位
    当量值折标系数
    当量值
    当量值单位
    等价值折标系数
    等价值
    等价值单位

【输出要求】
- 仅输出 JSON 数组
- 不要解释
- 不要 markdown
- 不要换行格式化
- 所有字段必须完整输出
- 缺失字段填 null
- 折标系数字段必须为数值类型或纯数字字符串（不得带单位）

文本：
{text}
""".strip()
