"""
电动机设备能效对标结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "motor_efficiency_benchmark_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的电动机设备信息、设备型号、数量、额定功率、效率及能效对标情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取与设备能效分析专家，擅长从企业文本或表格中提取电动机设备相关信息，并进行规范化结构输出。

你需要识别文本中的电动机设备参数，并提取对应的能效对标结果，而不是简单复制文本。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“电动机设备能效对标信息”，并输出为JSON数组。

【字段定义】
- 型号：设备型号
- 数量：设备数量
- 数量单位：如台、套等
- 额定功率：电动机额定功率
- 额定功率单位：如kW等
- 电机效率：设备实际效率或标称效率
- 电动机效率单位：如%等
- 一级能效值：一级能效标准值
- 二级能效值：二级能效标准值
- 三级能效值：三级能效标准值
- 能效对标结果：根据效率与标准对比得出的结果（如一级能效、二级能效、不达标等）

【抽取规则】
1. 一行数据对应一个电动机设备记录
2. 如果为表格，需按列映射字段
3. 如果为文本描述，需识别“型号 + 参数 + 效率 + 对标信息”
4. 数值保持原始值，不做计算或换算
5. 忽略汇总行（如“合计”“总计”）
6. 如果缺失字段，填 null
7. 如果文本未明确给出对标结果，可根据效率与标准描述进行合理判断（若无法判断则为 null）

【识别关键词】
电动机、电机、型号、功率、效率、能效、对标、标准

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    型号
    数量
    数量单位
    额定功率
    额定功率单位
    电机效率
    电动机效率单位
    1级能效值
    2级能效值
    3级能效值
    能效对标结果

文本：
{text}
""".strip()