"""
噪音监测信息结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "noise_monitoring_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的噪音检测信息、检测点位、昼夜噪声值、标准值、主要声源及达标情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业环境监测报告或检测记录中提取噪音监测相关信息，并进行规范化结构输出。

你需要识别不同检测点位的噪声数据，包括昼间值、夜间值、标准限值、主要声源及达标情况。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“噪音监测信息”，并输出为JSON数组。

【字段定义】
- 检测点位：噪声检测的具体位置或点位名称
- 检测点位编号：点位的编号或标识
- 检测日志：检测记录或日志信息（如日期、批次等描述）
- 昼间值：白天测得的噪声值
- 昼间值单位：如dB(A)等
- 夜间值：夜间测得的噪声值
- 夜间值单位：如dB(A)等
- 主要声源：产生噪声的主要来源（如设备、车间、交通等）
- 昼间值标准：昼间噪声限值标准
- 夜间值标准：夜间噪声限值标准
- 参考依据：检测所依据的标准或规范
- 达标情况：是否达标（如达标、不达标等）

【抽取规则】
1. 每个检测点位对应一条记录
2. 表格按列映射字段，文本按语义提取
3. 数值保持原始值，不做计算或换算
4. 标准值与参考依据需直接提取原文
5. 达标情况需直接提取或根据文本判断
6. 缺失字段填 null
7. 忽略汇总或重复描述

【识别关键词】
噪声、噪音、检测点、dB、昼间、夜间、声源、标准、达标、检测

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    检测点位
    检测点位编号
    检测日期
    昼间值
    昼间值单位
    夜间值
    夜间值单位
    主要声源
    昼间值标准
    夜间值标准
    参考依据
    达标情况

文本：
{text}
""".strip()