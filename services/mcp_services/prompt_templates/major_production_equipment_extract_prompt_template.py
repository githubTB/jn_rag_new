"""
主要生产设备信息结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "major_production_equipment_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}主要生产设备台账、设备清单、设备名称、型号规格、数量、功率、参数指标、生产线设备、主要生产设备"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业文本中提取生产设备相关信息。

你需要识别文本中主要生产的设备，并提取设备名称及其属性信息，输出列表数据。
""".strip()

# user 提示词模板
user_prompt = """
任务：
从文本中提取“主要生产设备信息”，并过滤相同设备信息，输出JSON数组。

【抽取目标】
识别企业用于生产的关键设备，如变压器、空压机、冷水机组、电动机、水泵、风机、锅炉或蒸汽发生器、空调等。

【字段定义】
- 设备名称：具体设备名称
- 数量：设备数量（如有）
- 规格型号：设备规格或型号（如有）
- 用途：设备在生产中的作用（如有）

【抽取规则】
1. 一个设备对应一条记录
2. 同一设备的属性需合并
3. 如果文本中存在多个设备，分别抽取
4. 忽略非生产相关设备（如办公设备、生活设施等）
5. 若字段缺失填 null

【识别关键词】
设备 / 生产设备 / 主要设备 / 关键设备 / 装置 / 机器 / 生产线

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 缺失字段填 null

输出格式：
设备名称
    型号
    数量
    数量单位
    所在工段
    设备类型

文本：
{text}
""".strip()
