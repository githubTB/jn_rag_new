"""
企业经营规模结构化抽取配置。
"""

SERVICE_NAME = "business_scale_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取{company_name}的经营规模情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业描述性文本中提取“经营规模相关指标”。

你需要基于语义理解字段含义，并进行标准化映射，而不是简单匹配关键词。
""".strip()

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：
从文本中提取企业“经营规模”相关数据，并输出JSON对象。

【字段定义与识别规则】

1. 员工总人数
   - 包含：员工总数、从业人数、在岗人数、职工人数等
   - 必须是“总量”

2. 生产人员数
   - 包含：生产人员、工人、一线员工等
   - 必须明确属于生产相关

3. 管理人员数
   - 包含：管理人员、行政人员、管理层等

4. 注册资本
   - 包含：注册资本、注册资金
   - 不包括：实缴资本（除非未区分）

5. 固定资产总值
   - 包含：固定资产、固定资产总额
   - 不包括：总资产、流动资产

【抽取规则】

1. 数值必须与字段语义匹配，不能误归类
2. 若字段未明确出现，填 null
3. 若存在单位（如万元、亿元、人），需保留在数值中或单独识别
4. 若同一字段出现多个值，优先选择：
   - 最新时间
   - 或最明确的一条

5. 忽略以下内容：
   - 模糊描述（如“大约”“约”可保留数值但不扩展）
   - 无法确定归属的数字

【输出格式】
    "员工总人数": number 或 null,
    "生产人员数": number 或 null,
    "管理人员数": number 或 null,
    "注册资本": number 或 null,
    "固定资产总值": number 或 null

要求：
- 仅输出JSON对象
- 不要解释
- 不要markdown
- 不要换行格式化

文本：
{text}
""".strip()