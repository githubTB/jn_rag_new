"""
温室气体排放信息结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "greenhouse_gas_emissions_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的温室气体排放信息、燃料燃烧排放、电力消耗排放及排放总量"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业碳排放或温室气体核算相关文本中提取排放数据，并进行规范化结构输出。

你需要识别燃料燃烧、电力消耗等来源的排放量，并提取对应单位及总排放量信息。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“温室气体排放信息”，并输出为JSON数组。

【字段定义】
- 燃料燃烧排放量：因燃料燃烧产生的温室气体排放量
- 燃料燃烧排放量单位：如tCO2e等
- 消耗电力排放量：因外购电力消耗产生的排放量
- 消耗电力排放量单位：如tCO2e等
- 排放总量：温室气体排放总量
- 排放总量单位：如tCO2e等

【抽取规则】
1. 每一类排放信息作为一条记录（如文本按年度或分类分段，需分别提取）
2. 表格按列映射字段，文本按语义提取
3. 数值保持原始值，不做计算或换算
4. 单位需原样提取
5. 缺失字段填 null
6. 忽略汇总或重复描述

【识别关键词】
温室气体、碳排放、CO2、tCO2e、燃料、电力、排放量、核算

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    年份
    燃料燃烧排放量
    燃料燃烧排放量单位
    消耗电力排放量
    消耗电力排放量单位
    排放总量
    排放总量单位

文本：
{text}
""".strip()