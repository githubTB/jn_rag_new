"""
用水结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "water_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取近三年的用水情况数据，包括自来水、地下水、地表水、循环水、再生水等水源类型的用水量、单位、回用量、排水量等信息，重点关注用水统计表、水平衡表、用水汇总表等"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业用水统计表、水平衡表、用水汇总表及相关说明文字中提取“近三年用水情况”数据。

你需要优先理解表格的行列关系，将“水源类型 + 年度”拆分成独立记录，并准确保留用水量、回用量、排水量及单位信息。
""".strip()

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：
从文本中提取“近三年用水情况”数据，并输出为JSON数组。

【字段定义】
- 水源类型：如自来水、地下水、地表水、循环水、再生水等
- 年度：如 2021、2022、2023、2024
- 用水量：原始用水数值
- 用水量单位：如 m3、t、万m3、L 等
- 回用量：循环使用或回收利用的水量
- 回用量单位：如 m3、t、万m3、L 等
- 排水量：废水或外排水量
- 排水量单位：如 m3、t、万m3、L 等

【抽取规则】
1. 每个“水源类型 + 年度”组合视为一条记录
2. 如果是表格，优先按表头与年份列进行映射；如果一行含多个年份，必须拆分成多条记录
3. 只提取用水相关内容，不要把能源消耗、原辅料消耗、污染物排放等非用水数据混入
4. 遇到“合计、总计、其中”这类汇总项时，仅在其本身明确属于用水总量时保留，否则忽略
5. 保持原始数值和单位，不做换算、不做补全推断
6. 缺失字段填 null

【识别关键词】
    水源类型
    年份
    用水量
    用水量单位
    回用量
    回用量单位
    排水量
    排水量单位

【输出要求】
- 仅输出 JSON 数组
- 不要解释
- 不要 markdown
- 不要换行格式化
- 所有字段都要输出
- 缺失字段填 null

输出字段结构：
    年份
    总用水量
    总用水量单位
    全厂新鲜用水量
    全厂新鲜用水量单位
    生产新鲜用水量
    生产新鲜用水量单位
    生活用新鲜水量
    生活用新鲜水量单位
    生产重复用水量
    生产重复用水量单位
    生产废水排放量
    生产废水排放量单位
    生产废水回用量
    生产废水回用量单位

文本：
{text}
""".strip()