"""
废气污染物排放总量情况结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "waste_gas_emission_total_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}的废气污染物排放总量情况、污染物名称、排放浓度、排放速率、年排放量及达标情况"

# system 提示词
system_prompt = """
你是一个结构化数据抽取专家，擅长从企业环保排放相关文本中提取废气污染物排放总量信息，并进行规范化结构输出。

你需要识别不同污染物的排放浓度、排放速率、年排放量、控制指标及达标情况，并进行结构化整理。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“废气污染物排放总量情况”，并输出为JSON数组。

【字段定义】
- 污染物名称：具体污染物名称（如颗粒物、SO2、NOx、VOCs等）
- 排放浓度：污染物排放浓度数值
- 单位：排放浓度对应单位（如mg/m³等）
- 排放标准：对应的排放标准限值或标准名称
- 排放速率：污染物排放速率
- 排放速率单位：如kg/h等
- 年排放量：污染物年排放总量的数值，不要单位
- 年排放量单位：如t/a、kg/a等
- 环评批复总量控制指标：环评或批复文件中的总量控制值
- 达标情况：是否达标（如达标、不达标等）

【抽取规则】
1. 每一种污染物对应一条记录
2. 表格按列映射字段，文本按语义提取
3. 数值保持原始值，不做计算或换算
4. 排放标准、控制指标需直接提取原文信息
5. 达标情况需从文本判断或直接提取
6. 缺失字段填 null
7. 忽略汇总或无关描述

【识别关键词】
污染物、排放浓度、排放速率、年排放量、标准、总量控制、环评、达标、废气、VOCs、颗粒物、SO2、NOx

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 所有字段必须完整
- 缺失字段填 null

输出字段结构：
    废气污染物名称
    年份
    年排放量
    年排放量单位
    环评批复总量控制指标

文本：
{text}
""".strip()