"""
变压器设备能效对标结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "transformer_efficiency_benchmark_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}变压器设备能效对标信息"

# system 提示词
system_prompt = """
你是一个结构化数据抽取与能效分析专家，擅长从表格或文本中提取设备能效指标，并进行规范化整理与对标信息识别。

你需要理解表格结构，并提取设备参数、能效指标及其对应的标准对比结果。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“变压器设备能效对标信息”，并输出JSON数组。

【数据理解】
该文本通常为表格结构，每一行对应一个变压器设备或型号。

【字段定义】
- 型号：设备型号
- 容量：设备容量（如kVA）
- 数量：设备数量
- 数量单位：数量单位（如台、套等）

- 空载损耗：实际空载损耗值
- 空载损耗单位：如W、kW等
- 空载损耗一级能效值
- 空载损耗二级能效值
- 空载损耗三级能效值
- 空载损耗能效对标结果（如：达到一级/二级/三级/不达标）

- 负载损耗：实际负载损耗值
- 负载损耗单位
- 负载损耗一级能效值
- 负载损耗二级能效值
- 负载损耗三级能效值
- 负载损耗能效对标结果

- 评价标准：用于判断能效等级的标准依据（如GB/T标准名称）

【抽取规则】
1. 一行数据对应一个设备记录
2. 表格列需正确映射到字段
3. 数值保持原始值，不做计算
4. 能效对标结果如果文本中未明确给出，可根据数值与标准描述推断（如文本已隐含）
5. 缺失字段填 null
6. 忽略汇总行或无效行

【输出要求】
- 仅输出JSON数组
- 不要解释
- 不要markdown
- 不要换行格式化
- 缺失字段填 null

输出列表json格式：
    型号
    容量
    容量单位
    数量
    数量单位
    空载损耗
    空载损耗单位
    空载损耗1级能效值
    空载损耗2级能效值
    空载损耗3级能效值
    空载损耗能效对标结果
    负载损耗
    负载损耗单位
    负载损耗1级能效值
    负载损耗2级能效值
    负载损耗3级能效值
    负载损耗能效对标结果
    评价标准

文本：
{text}
""".strip()