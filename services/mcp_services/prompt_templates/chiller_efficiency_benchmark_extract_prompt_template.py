"""
冷水机组设备能效对标结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "chiller_efficiency_benchmark_extract_prompt_template"

# 检索问题/抽取主题默认值（RAG搜索词）
q = "提取{company_name}冷水机组设备能效对标信息"

# system 提示词
system_prompt = """
你是一个结构化数据抽取与设备能效分析专家，擅长从表格或半结构化文本中提取冷水机组设备参数及能效对标信息。

你需要理解表格结构，将每一行设备信息转换为结构化数据，并提取对应的能效对标结果。
""".strip()

# user 提示词
user_prompt = """
任务：
从文本中提取“冷水机组设备能效对标信息”，并输出为JSON数组。

【数据理解】
该文本通常为表格结构，每一行对应一个冷水机组设备或型号。

【字段定义】

- 型号：设备型号
- 数量：设备数量
- 数量单位：如台、套等

- 名义制冷量：设备的名义制冷能力
- 名义制冷量单位：如kW、RT等

- 功率：设备输入功率
- 功率单位：如kW等

- 制冷机组COP：性能系数（Coefficient of Performance）
- 制冷机组COP单位：通常为无单位或比值

- 一级能效值：一级能效标准值
- 二级能效值：二级能效标准值
- 三级能效值：三级能效标准值

- 能效对标结果：根据实际COP与标准值对比得出的结果（如一级能效、二级能效、不达标等）

【抽取规则】
1. 一行数据对应一个设备记录
2. 表格列需映射到对应字段
3. 数值保持原始值，不进行计算
4. 如果文本中未明确给出能效对标结果，可根据已有描述判断
5. 忽略汇总行或无效行
6. 缺失字段填 null

【识别关键词】
型号 / 制冷量 / 功率 / COP / 能效 / 标准 / 对标 / 机组

【输出要求】
- 输出为JSON数组
- 每条记录包含所有字段
- 不要解释
- 不要markdown
- 不要换行格式化
- 缺失字段填 null

输出格式：
每条记录包含字段：
    型号
    数量
    数量单位
    名义制冷量
    名义制冷量单位
    功率
    功率单位
    制冷机组COP
    制冷机组COP单位
    1级能效值
    2级能效值
    3级能效值
    能效对标结果
文本：
{text}
""".strip()